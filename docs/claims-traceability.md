# Claims Traceability

Cross-check date: 2026-07-25

Documents checked:

- `docs/description-yicte.md`
- `docs/description-sts.md`

The table maps the ten historical sentence corrections in
`audit/alignment_report.md` to current behavior. Items 6, 8, and 10 retain
stronger wording because later fixes resolved the audited limitation.

| Audit item | Current claim or boundary | Code evidence | Test evidence |
|---|---|---|---|
| 1. Verification | Both descriptions report the final Python 3.12 pass/warning counts without a measured elapsed time. | The verification command is `cd backend && AZURE_OPENAI_ENABLED=false pytest -q`. This is run evidence, not an application feature. | Final full suite: `232 passed`; warning count: `579`. |
| 2. AI-review decisions | The primary AI-review page displays contributor-original, current reviewed, and AI-proposed values and offers per-indicator accept/edit/reject controls. Explicit per-indicator decisions are not universal: the authenticated compatibility finalizer synthesizes a decision, and the primary POST permits omitted indicator arrays. | `observation_ai_review`, `observation_create_risk_case`, and `observation_create_case` in `backend/app/main.py`; controls in `backend/app/templates/ai_review_result.html`. | `test_ai_review_page_shows_original_current_and_ai_values`, `test_ai_review_finalize_creates_risk_case_with_overrides`, and `test_legacy_create_case_route_preserves_ai_proposal_and_snapshots_final` in `backend/tests/test_ai_review_flow.py`. |
| 3. Snapshot stability and immutability boundary | Snapshotted cases are application-level write-once through routes, and case/report views remain stable after Observation edits. `final_snapshot` is nevertheless an ordinary nullable JSON column, and referenced image files are not database- or content-immutable. | `RiskCase.final_snapshot` in `backend/app/models.py`; snapshot construction/read in `backend/app/provenance.py`; case/report rendering in `backend/app/main.py`, `backend/app/reports.py`, and case templates. | `test_original_and_ai_proposal_survive_review_edit_finalize_cycle`, `test_post_case_observation_edit_cannot_change_case_or_reports`, and snapshot-only report tests in `backend/tests/test_provenance.py` and `backend/tests/test_reports.py`. |
| 4. Status transitions and seed exception | The normal reviewer route enforces Draft → Needs Review; Needs Review → Verified or Draft; Verified → Routed or Needs Review; Routed → Closed; Closed is terminal. Routed needs a destination and accepted changes append `CaseEvent`. Built-in demo seeding directly inserts later statuses without transition events. | `ALLOWED_TRANSITIONS` in `backend/app/case_status.py`; `case_update_status` in `backend/app/main.py`; `CaseEvent` in `backend/app/models.py`; direct status assignment in `backend/app/seed.py`. | `backend/tests/test_case_status_transitions.py` covers every allowed transition, rejected transitions, destination validation, terminal Closed, and event rendering. `test_03_seed` in `backend/tests/test_mvp_smoke.py` exercises built-in seeding; there is no dedicated assertion that its later statuses have zero events. |
| 5. Sensitive evidence boundary | Sensitive image, note, and tag evidence is redacted in selected general views. Observation metadata can remain visible, and `/uploads` plus read-only observation/case/report routes are not access-controlled. | Sensitive branches in `backend/app/templates/observation_detail.html`, `site_detail.html`, and `index.html`; public `/uploads` mount and read-only GET routes in `backend/app/main.py`. | `test_sensitive_observation_is_hidden_from_general_views` in `backend/tests/test_review_actions.py` verifies selected-view redaction and reviewer visibility. It does not establish private file delivery. |
| 6. Prompt and rendered-request hashes — resolved | New sessions store `prompt_template_sha256` over the rendered system prompt, deployment, and transmitted request settings, plus `rendered_request_sha256` over the canonical per-session Azure-shaped request including notes, image ids, and image bytes. Only `max_completion_tokens=600` is recorded/sent; temperature is absent. Raw requests are not stored. Mock hashes identify would-be requests, and legacy rendered hashes remain `NULL`. | `PROMPT_SETTINGS`, `analysis_prompt_template_sha256`, `build_analysis_request_payload`, `analysis_rendered_request_sha256`, and the exact `create(**request_payload)` call in `backend/app/services/providers/azure_openai_provider.py`; fields/migration in `backend/app/models.py` and `backend/app/database.py`; persistence/export in `scripts/run_experiment.py` and `scripts/export_results.py`. | `TestProviderValidation::test_successful_azure_response_returns_deployment_provider` in `backend/tests/test_ai.py`; hash/settings assertions in `backend/tests/test_experiment_scripts.py`; legacy migration assertions in `backend/tests/test_database_migration.py`. |
| 7. Pilot size | `scripts/select_assets.py` selects complete groups whose privacy and cultural-sensitivity statuses are cleared, then writes a seeded pilot of up to six eligible assets and a held-out list. Six is the default maximum, not a guaranteed real-corpus count. | `_photo_is_cleared`, `_complete_groups`, `select_assets`, and `--pilot-size` in `scripts/select_assets.py`. | `test_select_assets_excludes_uncleared_and_splits_deterministically` in `backend/tests/test_corpus_scripts.py` verifies exclusions, determinism, and a synthetic 6-pilot/2-held-out split. |
| 8. Held-out execution — resolved | Split JSON manifests run directly with `--asset-set pilot` (default), `held_out`, or `all`; every new session records the selected set. No copying into a separate `assets` array is required. | `ASSET_SETS`, `_load_manifest`, `run_experiment`, and CLI parsing in `scripts/run_experiment.py`. | `test_run_experiment_selects_pilot_held_out_and_all_asset_sets` in `backend/tests/test_experiment_scripts.py` verifies 2 pilot → 4 sessions, 3 held-out → 6, all 5 → 10, and stored set names. |
| 9. Inter-rater agreement and blinding | The human-reference CSV accepts overlapping labels from two reviewer IDs and the analysis computes Cohen's kappa. Assignment and blinding are external and unrecorded. | `load_human_reference`, `_double_labelled_pairs`, and `cohen_kappa` in `research/analysis/metrics.py`. | `test_paired_delta_kappa_confidence_and_repeatability_known_answers` in `backend/tests/test_research_analysis.py` verifies hand-computed kappa. No software test can establish an external blinding procedure. |
| 10. Native repeatability — resolved as tooling | `--repeat-runs N` creates zero-based repeated sessions under `(asset_id, condition, run_index)` and resumes per index. Exports/loaders retain repeats; repeatability uses all runs, including empty-indicator sessions, while primary condition metrics use run index 0. | `AssessmentSession.run_index` and its unique constraint in `backend/app/models.py`; migration in `backend/app/database.py`; repeat/resume logic in `scripts/run_experiment.py`; export in `scripts/export_results.py`; `load_ai_export`, `repeatability`, and `analyze_experiment` in `research/analysis/metrics.py`. | `test_startup_migration_rebuilds_legacy_assessment_session_constraint`, `test_repeat_runs_create_four_sessions_and_resume_by_run_index`, and `test_loader_preserves_repeated_runs_for_hand_computed_agreement`. This is synthetic tooling evidence, not a completed real experiment. |
| Fix 1. Provider identity | One classifier recognizes legacy `azure_openai`, current `azure:<deployment>`, and `mock`; deployment-qualified Azure results remain Azure at finalization and in snapshots/reports. | `provider_identity` and `azure_provider_name` in `backend/app/provider_identity.py`; uses in `backend/app/main.py`, `backend/app/reports.py`, `scripts/run_experiment.py`, and `scripts/verify_azure.py`. | `backend/tests/test_provider_identity.py` and `test_deployment_qualified_azure_provider_stays_azure_at_finalization` in `backend/tests/test_ai_schema_v2.py`. |
| Fix 2. Azure failure history | New operational Azure configuration/import/image/transport/API/provider failures append a sanitized failed Azure attempt before the separately labelled active mock result. Malformed JSON/schema validation remains one failed record without mock fallback. | `AIAnalysisAttempt` and `_mock_after_azure_failure` in `backend/app/services/ai_analysis.py`; provider failure branches in `backend/app/services/providers/azure_openai_provider.py`; `AIAnalysisRecord` in `backend/app/models.py`; ordered persistence/snapshotting in `backend/app/main.py` and `backend/app/provenance.py`. | `test_connection_failure_persists_failed_then_mock_and_renders` and `test_malformed_json_remains_one_failed_record_without_mock` in `backend/tests/test_ai_failure_history.py`. |
| Fix 4. Site concentration | An optional redacted `site_label` flows from the experiment manifest through `ExperimentAsset` and both CSV row types. Analysis counts each labelled physical asset once and reports site concentration when labels exist. | `ExperimentAsset.site_label`; migration in `backend/app/database.py`; persistence/export in `scripts/run_experiment.py` and `scripts/export_results.py`; `load_ai_export` and `site_concentration` in `research/analysis/metrics.py`; Markdown in `research/analysis/reporting.py`. | `test_run_experiment_mock_creates_paired_sessions_and_exports` and `test_site_concentration_flows_through_loader_and_results` verify a 3-asset/2-site count, a two-thirds largest-site share, and non-NaN Markdown output. |
| File signatures and metadata | JPEG/PNG/WEBP uploads are checked by suffix and signature, size-limited, orientation-corrected, and metadata-stripped before storage. | `image_format_from_signature`, `sanitize_image_content`, and `save_upload_image` in `backend/app/main.py`. | `backend/tests/test_upload_security.py` covers renamed PNG rejection, text-as-PNG rejection, oversize rejection, EXIF/GPS removal, and orientation. |
| Review queue | Logged-out public submissions enter `Pending`; reviewer decisions and analysis are separate guarded steps. | `HumanReviewStatus` and public/reviewer routes in `backend/app/models.py` and `backend/app/main.py`; review templates. | `backend/tests/test_review_queue.py`, `backend/tests/test_review_actions.py`, and `test_public_submission_works_logged_out_and_stays_pending`. |
| Authentication and CSRF | Reviewer actions use one configured credential and a signed session; state-changing forms require CSRF. Public submission remains unauthenticated by design. | `backend/app/auth.py`; session middleware, dependencies, and routes in `backend/app/main.py`. | `test_guarded_get_routes_redirect_logged_out`, `test_guarded_post_routes_redirect_logged_out`, `test_logged_out_analysis_never_invokes_analyzer`, and `test_authenticated_form_without_csrf_is_rejected` in `backend/tests/test_reviewer_auth.py`. |
| Legacy route removal | The unauthenticated legacy single-image site upload route/template is absent; active intake uses public multi-image submission or authenticated reviewer-led intake. | Route inventory in `backend/app/main.py`; `backend/app/templates/observation_new.html` is absent. | Auth, smoke, site-intake, and upload-security tests exercise only the supported paths. |

Required-word cross-check:

| Term | Result |
|---|---|
| `immutable` / `frozen` | Bare claims are intentionally absent. The descriptions use qualified application-level write-once and two-hash wording instead. |
| `signature`, `review queue`, `authenticated`, `audit`, `removed` | Present in the descriptions and mapped above. |

Remaining disclosed boundaries include one shared credential, no review
timestamp or complete edit log, authenticated intake/seed paths that bypass
`Pending`, public media/report routes, seeded later statuses without events,
external blinding, no committed live-Azure or real-corpus results, and
unvalidated heuristic risk weights.
